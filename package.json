{
  "name": "terraform-provider-mezmo",
  "version": "0.0.0",
  "description": "A Terraform provider for Mezmo's Pipeline product",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mezmo/terraform-provider-mezmo.git"
  },
  "scripts": {
    "commitlint": "commitlint --from=origin/main",
    "local": "env $(cat ./env/local.env) "
  },
  "commitlint": {
    "extends": "@answerbook/commitlint-config-logdna"
  },
  "devDependencies": {
    "@answerbook/commitlint-config-logdna": "^2.2.1"
  }
}
